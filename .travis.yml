language: dart

dart:
  - dev

branches:
  only: [master]

dart_task:
  - dartfmt
  - test
  - dartanalyzer

scripts:
  # - pub get
  - pub run test_coverage

after_success:
  - bash <(curl -s https://codecov.io/bash)

cache:
  directories:
    - $HOME/.pub-cache
    - .dart_tool
